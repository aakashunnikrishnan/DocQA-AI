apiVersion: v1
kind: Service
metadata:
  name: {{ include "docqa-ai.fullname" . }}
  labels:
    {{- include "docqa-ai.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.config.monitoring.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "docqa-ai.selectorLabels" . | nindent 4 }}
